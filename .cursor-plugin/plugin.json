{
  "name": "string-web-access",
  "displayName": "String Web Access",
  "version": "1.0.3",
  "minClientVersions": {
    "cursor": "3.13.0"
  },
  "description": "Search the web, fetch any URL or send it a write request, and map a site's URLs — all returned as clean, LLM-ready Markdown for Cursor. Proxy rotation, anti-bot handling, CAPTCHA solving and JavaScript rendering happen server-side, so the agent gets the page instead of a block screen. Best for sites that rate-limit, geo-gate or block automated traffic.",
  "author": {
    "name": "String",
    "email": "support@usestring.ai"
  },
  "homepage": "https://portal.usestring.ai/docs/mcp/overview",
  "repository": "https://github.com/usestring/string-cursor-plugin",
  "license": "MIT",
  "logo": "assets/logo.png",
  "keywords": [
    "mcp",
    "model-context-protocol",
    "mcp-server",
    "string-ai",
    "web-access",
    "web-scraping",
    "web-search",
    "markdown",
    "anti-bot",
    "captcha",
    "proxy",
    "crawler",
    "data-extraction",
    "llm",
    "ai-agents",
    "claude",
    "cursor",
    "unblocker",
    "string",
    "string web access",
    "usestring",
    "crawl"
  ],
  "category": "integrations",
  "tags": [
    "string",
    "web-search",
    "web-scraping",
    "mcp"
  ],
  "skills": "./skills/",
  "rules": "./rules/",
  "commands": "./commands/",
  "mcpServers": "./mcp.json",
  "variables": {
    "type": "object",
    "properties": {
      "STRING_API_KEY": {
        "type": "string",
        "title": "String API key",
        "description": "Your String Web Access API key, sent as a bearer token to https://mcp.usestring.ai/v1/mcp. Create one at https://portal.usestring.ai."
      }
    },
    "required": [
      "STRING_API_KEY"
    ]
  }
}
