{
  "name": "string-web-access",
  "displayName": "String Web Access",
  "version": "1.0.0",
  "description": "Fetch, search and map any website as clean Markdown. Handles proxy rotation, anti-bot protection and CAPTCHAs, so the agent gets the page instead of a block screen.",
  "author": {
    "name": "String",
    "email": "support@usestring.ai",
    "url": "https://usestring.ai"
  },
  "homepage": "https://portal.usestring.ai/docs",
  "repository": "https://github.com/usestring/string-cursor-plugin",
  "license": "MIT",
  "logo": "assets/logo.png",
  "keywords": [
    "string",
    "string web access",
    "usestring",
    "web-scraping",
    "web-search",
    "markdown",
    "anti-bot",
    "crawl",
    "mcp"
  ],
  "skills": "./skills/",
  "rules": "./rules/",
  "commands": "./commands/",
  "mcpServers": "mcp.json",
  "variables": {
    "type": "object",
    "properties": {
      "STRING_API_KEY": {
        "type": "string",
        "title": "String API key",
        "description": "Your String Web Access API key, sent as a bearer token to https://mcp.usestring.ai/v1/mcp. Create one at https://portal.usestring.ai."
      }
    },
    "required": [
      "STRING_API_KEY"
    ]
  }
}
